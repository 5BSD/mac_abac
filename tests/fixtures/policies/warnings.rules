# Rules that generate warnings but are still valid

# Transition without newlabel (warning: no label to transition to)
transition exec * -> type=setuid

# Another valid rule
allow exec * -> *

# Transition with exec on non-exec operation (warning but accepted)
transition read * -> type=data => type=elevated
