# Invalid syntax file for testing error handling

# Missing arrow
allow exec type=app type=data

# Invalid action
permit exec * -> *

# Missing operation
allow * -> *

# This line is valid (to test partial parsing)
allow exec * -> *

# Another invalid line
deny -> type=foo
