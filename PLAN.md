Todos:

Am I handling the rule ID field correctly? Is it useful? Would another datastructure or way to define how one rule beats another is needed. Could I remove rules or pull rules out? Would anything be useful to maybe try and associate rules with a group? A set of rules that can be managed on a per process basis? Right now they are prirotiy order and I'm not saying that bad but I don't see how nubmering them is going to do any good? Or I guess I could read the file or if I saved the rule read that and be able to use the number to identify a rule? WOuld a group like thing make more sense?

Dtrace probes ont eh critical entry and exit points that add the probe arguments.

Could we move to a multi rule engine model?

What about ym limits do they make sense now that we are not usign iocvtl?

Improve the test suite. Make sure all new features are tested.

FreeBSDKit: Label tool doesn;t support context.

We have add. What about subtract? WOr we do a clear load?